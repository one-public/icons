import { resolve } from "node:path";

export const cacheDir = 'cache'; 
export const iconPrefix = 'ha';

/** @hatech/icon-json 项目路径 */
export const iconJsonPath = resolve('../json')

/** @hatech/icon-svgs 项目路径 */
export const iconSvgsPath = resolve('../svgs')