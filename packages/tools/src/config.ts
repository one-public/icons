import { resolve } from "node:path";

export const cacheDir = 'cache'; 
export const iconPrefix = 'one-icons';

/** @one-public/icons 项目路径 */
export const iconsPath = resolve('../icons')

/** @one-public/icon-svgs 项目路径 */
export const iconSvgsPath = resolve('../svgs')
